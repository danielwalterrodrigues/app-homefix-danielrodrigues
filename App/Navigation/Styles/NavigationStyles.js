import { StyleSheet } from 'react-native'
import { Colors, Metrics, Fonts } from '../../Themes/'

export default StyleSheet.create({
  header: {
    backgroundColor: Colors.backgroundMenu,
    // borderWidth: 3
  },
  tabLabel: Fonts.style.tabLabel,
  containerStyle: {
    paddingTop: 5,
    paddingBottom: 5,
    height: 60,
    // width: 100,
    backgroundColor: '#ffffff',
    // borderWidth: 1
  },
  tabStyles: {
    /*borderTopWidth: 1,
    borderTopColor: 'red'*/
  }
})
 